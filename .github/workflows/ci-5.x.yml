name: vert.x (5.x)
on:
  push:
    branches:
      - enable-io-uring-ci
  pull_request:
    branches:
      - enable-io-uring-ci
jobs:
  CI-CD:
    uses: ./.github/workflows/ci-matrix-5.x.yml
    secrets: inherit
    with:
      branch: ${{ github.event.pull_request.head.sha || github.ref_name }}
